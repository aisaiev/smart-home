---
  - name: "Pico Flower Led"
    unique_id: "pico_flower_led"
    state_topic: "pico-flower-led/state"
    state_value_template: "{{ value_json.power }}"
    brightness_state_topic: "pico-flower-led/state"
    brightness_value_template: "{{ value_json.brightness }}"
    brightness_scale: 100
    command_topic: "pico-flower-led/cmnd/power"
    payload_on: "on"
    payload_off: "off"
    brightness_command_topic: "pico-flower-led/cmnd/brightness"